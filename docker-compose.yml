version: "3.8"
services:
  backend:
    build: ./backend
    container_name: ecev-backend
    ports:
      - "8001:8000"
    volumes:
      - ./config:/app/config
      - db-data:/app/data
    environment:
      - CONFIG_PATH=/app/config/config.json

  frontend:
    build: ./frontend
    container_name: ecev-frontend
    ports:
      - "3001:80"
    depends_on:
      - backend

volumes:
  db-data:
